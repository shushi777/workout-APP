# Railway Nixpacks Configuration
# This file tells Railway which system packages to install

[phases.setup]
# Install FFmpeg and OpenCV system dependencies
aptPkgs = [
    "ffmpeg",
    "libgl1-mesa-glx",
    "libglib2.0-0",
    "libsm6",
    "libxext6",
    "libxrender-dev",
    "libgomp1"
]
